# 老师项目要求（期末作业）

> 来源：老师发布的 Final Project 要求截图。本文为便于项目实施而整理的中文版本；如与老师后续通知不一致，以老师通知为准。

## 1. 作业目标

期末项目由 **原型（Prototype）+ 展示（Presentation）** 构成。需要运用并行计算和／或分布式计算，相比串行基线提升：

- 性能；
- 可扩展性；
- 效率。

项目应清楚说明计算应在何处执行，例如：

- 单核 CPU：串行基线；
- 多核 CPU：并行计算；
- 多节点：分布式计算；
- GPU：CUDA 加速。

## 2. 必选技术组成

项目至少包含下列 **两类** 技术组成，其中至少一类必须是并行计算或 GPU 加速：

| 技术组成 | 示例技术 |
| --- | --- |
| CPU 并行计算 | OpenMP、MPI |
| 分布式计算 | Spark、Hadoop、Cassandra、Kafka |
| GPU 加速 | CUDA |

## 3. 项目说明必须包含的内容

针对所定义的项目，需要清晰描述：

1. 问题定义；
2. 需要解决的计算问题或数据处理问题；
3. 为什么该问题能从并行、分布式或 GPU 加速中获益；
4. 预期数据规模或计算强度。

还必须提供一个清晰的 **串行 CPU 基线实现**，并指出其中的计算瓶颈。

## 4. 实现要求

### 4.1 并行或 GPU 部分

并行／GPU 计算部分至少采用以下一种方式实现：

- OpenMP：共享内存并行；
- MPI：分布式内存并行；
- CUDA：GPU 加速计算。

### 4.2 分布式部分（如项目包含）

如果项目包含分布式计算，至少使用一种分布式系统，例如 Spark、Hadoop（批处理）、Cassandra（分布式存储）或 Kafka。

需要说明：

- 为什么选择该系统；
- 数据如何在系统中流动；
- 它如何与并行计算组件集成。

## 5. 架构图与性能分析

需要给出清晰的架构图，展示：

`数据接入 → 存储 → CPU 并行／GPU 计算 → 输出或结果`

重点是概念清晰，不要求展开基础设施细节。

必须分析以下指标：

- 执行时间；
- 加速比；
- 吞吐量；
- 延迟（分布式组件适用时）。

并比较：

- 串行与并行；
- CPU 与 GPU；
- 本地执行与分布式执行（适用时）。

## 6. 展示要求

- 展示时长：15～20 分钟；
- 展示形式：幻灯片 + Demo + 代码讲解；
- 必须现场出席并展示；如有有效请假理由，需要在考试前联系 Examination Office，并抄送老师且提供证明材料。

## 7. 与 ParallelPix 项目的对应

当前项目可按以下方式满足要求：

| 老师要求 | ParallelPix 对应实现 |
| --- | --- |
| 串行 CPU 基线 | C++ 单线程逐像素图像处理 |
| CPU 并行计算 | C++ + OpenMP 多线程逐像素处理 |
| GPU 加速 | CUDA Kernel 实现相同像素处理 |
| 两类技术组成 | OpenMP（CPU 并行）+ CUDA（GPU 加速） |
| 性能对比 | 串行、OpenMP、CUDA 的执行时间、加速比、吞吐量；CUDA 另报告传输与 Kernel 时间 |
| 数据与结果 | 图像目录作为输入，处理后图片和 Benchmark CSV／图表作为输出 |

本项目不一定需要加入分布式系统：选择 **OpenMP + CUDA** 已满足“至少两类技术组成”的要求。若后续加入 Spark、Kafka 等分布式组件，则还需要补充本地与分布式执行的对比及延迟／吞吐量分析。

计划中的 M8 CPU-GPU Hybrid 属于 OpenMP 与 CUDA 之上的异构调度扩展，用于研究两种计算资源同时处理同一图片批次能否进一步缩短时间。它不替代独立的 OpenMP/CUDA 实现，也不是满足“两类技术组成”所必需的模块。

## 8. 重要限制

OpenCV 可以用于图片读取、保存和 `cv::Mat` 等数据容器；核心图像处理必须自行按像素实现，不能直接使用 OpenCV 现成的图像处理算法替代课程要求的实现。例如，不应以 `cv::resize`、`cv::cvtColor` 等函数代替自己的串行、OpenMP 和 CUDA 核心计算逻辑。
